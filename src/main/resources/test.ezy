/*a : number = 10
println(a * 2)
println((a*2))*/

a: number = 10
println(1+2 * a)

/*arr: number[] = [5, 2, 3, 4, 1]

print("정렬 전: ")
for (beforeSort: number in arr) {
  print("${beforeSort} ")
}

n: number = arr.length
temp: number = 0
for (i: number in 0 .. n - 2) {
    for (j: number in 0 .. n - i - 2) {
        if (arr[j] >= arr[j + 1]) {
            temp = arr[j]
            arr[j] = arr[j + 1]
            arr[j + 1] = temp
        }
    }
}

print("\n정렬 후: ")
for (afterSort: number in arr) {
  print("${afterSort} ")
}*/

